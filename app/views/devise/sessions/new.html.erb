<style>
  .btn-login {
    background-color: transparent;
    border: none;
    border-radius: 999px;
    padding-left: 30px;
    padding-right: 30px; 
    margin: 0;
    font-size: 18px;
    color: #143196;
    box-shadow: inset 0 0 0 1px #143196;
    cursor: pointer;
  }
  .btn-login:hover {
    background-color: #143196;
    color: white;
  }
</style>
<div class="container-fluid background" style="padding-top: 70px">
  <div class="container form-box">
    <h3 class="text-center" style="font-size: 65px; font-weight: 300">Login</h3>
    <h5 class="text-center" style="padding-top: 10px; font-size: 20px; font-weight: 300"">Novo na Huubster? <%= link_to "Registra-te", new_registration_path(resource_name) %></h5>
    <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
      <div class="form-inputs">
        <%= f.input :email, required: false, autofocus: true %>
        <%= f.input :password, required: false %>
        <%= f.input :remember_me, as: :boolean if devise_mapping.rememberable? %>
        <%= link_to "Esqueceste a tua password?", new_password_path(resource_name) %>
      </div>

      <div class="btn-div">
        <%= f.button :submit, "Login", :class => "btn-login" %>  
      </div>
    <% end %>

    <div class="container-fluid">
      <p class="text-center" style="font-weight: 300">* Ao fazer login, concordas com os nossos <%= link_to 'Termos de Uso', terms_path %> e com a nossa <%= link_to 'Política de Privacidade', privacy_path %>. Também aceitas o uso de cookies da Huubster para te oferecer a melhor experiência enquanto usuário.</p>
    </div>
  </div>
</div>